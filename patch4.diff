--- packages/auth/src/platform_browser/util/worker.ts
+++ packages/auth/src/platform_browser/util/worker.ts
@@ -16,13 +16,15 @@
  * limitations under the License.
  */

-// Declare WorkerGlobalScope to avoid TypeScript errors in environments without it.
-declare var WorkerGlobalScope: any;
+interface SelfWithWorkerGlobalScope {
+  WorkerGlobalScope?: unknown;
+}

 export function _isWorker(): boolean {
   return (
     typeof self !== 'undefined' &&
-    typeof (self as any).WorkerGlobalScope !== 'undefined'
+    typeof (self as unknown as SelfWithWorkerGlobalScope)
+      .WorkerGlobalScope !== 'undefined'
   );
 }
