import { SourceFile } from 'ts-morph';

/**
 * Substitutes references to unexported internal types with their public base classes or aliases.
 */
export function substitutePrivateTypeReferences(sourceFile: SourceFile): void {}
