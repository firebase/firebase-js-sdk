import type { RollupReplaceOptions } from 'rollup';

export function generateBuildTargetReplaceConfig(moduleFormat: string, languageTarget: number): RollupReplaceOptions;
export const BUILD_TARGET_MAGIC_STRING: string;
