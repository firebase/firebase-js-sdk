name: Run All Tests

on:
  pull_request:
    branches:
      - master
    types:
      - closed
  schedule:
    - cron: '0 6,18 * * *'

jobs:
  test:
    name: Node.js and Browser (Chrome) Tests
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - run: echo 'Triggered from closing pull request without merge?'
